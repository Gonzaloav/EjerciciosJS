// Para que imprima tabla de multiplicar.

for (let outer = 0; outer < 7 ; outer++) {
    for (let inner = 0; inner < 7; inner++ ){
        console.log(`Outer: ${outer}, Inner: ${inner}, Resultado: ${outer * inner}`);
    }
}